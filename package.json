{
  "name": "hydrallm-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HydraLLM 项目展示示例站 - Cloudflare Pages 静态站点",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "build:ui": "cd ui && npm ci 2>/dev/null || true && npm run build",
    "build:all": "npm run build && npm run build:ui",
    "preview": "vite preview",
    "deploy": "npm run build:all && wrangler pages deploy dist --project-name hydrallm-demo"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@iconify-json/lucide": "^1.2.18",
    "@unocss/preset-icons": "^0.65.3",
    "@unocss/preset-uno": "^0.65.3",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.2",
    "unocss": "^0.65.3",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0",
    "wrangler": "^3.99.0"
  }
}
