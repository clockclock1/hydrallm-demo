# Cloudflare Pages SPA fallback —— 上游 HydraLLM UI 使用浏览器路由（history.pushState）
# 每个管理页面有独立 URL：/ui/dashboard, /ui/providers, /ui/chains 等。
# 直接访问或刷新这些 URL 时，需要 fallback 到 /ui/index.html 让 React 接管路由。
#
# 规则必须放在构建输出目录根（dist/_redirects = public/_redirects），CF Pages 才会识别。
/ui/*  /ui/index.html  200
