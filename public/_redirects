# Cloudflare Pages SPA fallback — 上游 HydraLLM React UI 使用浏览器路由（history.pushState）
#
# 问题背景：
# React UI 的 App.tsx 用 window.location.pathname 读取路由（如 /dashboard），
# 并用 history.replaceState() 把 URL 从 /ui/dashboard 改写为 /dashboard（无 /ui/ 前缀）。
# 因此需要把根级别的 8 个页面路径也 fallback 到 React UI 入口。
#
# 访问流程：
# 1. 用户从首页点「体验演示」→ /ui/dashboard
# 2. _redirects /ui/* → /ui/index.html (200) → React UI 加载
# 3. React UI replaceState({page}, '', '/dashboard') → URL 变成 /dashboard
# 4. 用户刷新 /dashboard → _redirects /dashboard → /ui/index.html (200) → React UI 加载并正确显示
#
# 规则顺序重要：详细路径在前，通配符在后。

# React UI 自身的浏览器路由页面（根级别，无 /ui/ 前缀）
/dashboard     /ui/index.html   200
/providers     /ui/index.html   200
/model-tests   /ui/index.html   200
/chains        /ui/index.html   200
/model-stats   /ui/index.html   200
/endpoints     /ui/index.html   200
/live-status   /ui/index.html   200
/logs          /ui/index.html   200

# 通配：所有 /ui/* 路径 fallback 到 React UI 入口（保证 /ui/dashboard 等路由可用）
/ui/*  /ui/index.html  200